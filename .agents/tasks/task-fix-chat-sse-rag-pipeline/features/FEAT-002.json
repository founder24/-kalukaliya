{
  "id": "FEAT-002",
  "type": "fix",
  "description": "Address review issues #1, #3, #5: fix streaming endpoint serialized history load, extract duplicated scoring logic into helper, and lower heuristic fallback score from 0.85 to 0.65.",
  "status": "in_progress",
  "steps": [
    "Step 1 - Fix streaming endpoint to use asyncio.gather in both branches (Issue #3)",
    "Step 2 - Extract _extract_score helper method in vertex_search.py (Issue #5)",
    "Step 3 - Lower heuristic fallback score from 0.85 to 0.65 (Issue #1)"
  ],
  "acceptance_criteria": [
    "Streaming endpoint uses asyncio.gather for both generic and non-generic query branches",
    "vertex_search.py has a _extract_score helper used in both primary and fallback loops",
    "Heuristic score for content >50 chars is 0.65 instead of 0.85",
    "All existing tests pass"
  ],
  "verification": [
    "Run `cd /projects/sandbox/-kalukaliya/apps/backend && python -m pytest tests/ -x -q` and confirm all tests pass"
  ],
  "blocked_reason": null,
  "findings": null
}
