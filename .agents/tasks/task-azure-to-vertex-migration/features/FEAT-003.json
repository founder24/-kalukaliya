{
  "id": "FEAT-003",
  "type": "chore",
  "description": "Update backend tests to reflect the Azure-to-Vertex migration. Fix test imports, mocks, and assertions that reference Azure Search. Update backend Dockerfile comment if needed. Ensure all tests pass.",
  "status": "pending",
  "steps": [
    "Step 1: Update apps/backend/tests/test_latency_audit.py - Change azure_search references to vertex_search. Update test_azure_search_has_warm_up_method to test_vertex_search_has_warm_up_method, importing from app.services.search.vertex_search.",
    "Step 2: Update apps/backend/tests/test_latency_comparison.py - Change all azure_search_cold_start references and comments to vertex_search.",
    "Step 3: Update apps/backend/tests/test_pipeline_audit.py - Change the mock path from 'app.services.search.azure_search.search_service.search_context' to 'app.services.search.vertex_search.search_service.search_context'.",
    "Step 4: Update apps/backend/tests/test_service_degradation.py if it references Azure Search - change to Vertex Search.",
    "Step 5: Update any other test files that import or mock azure_search (check test_chat.py, test_english_chat_integration.py).",
    "Step 6: Run pytest apps/backend/ --tb=short -q to verify tests pass (allowing known failures for missing DB/Redis in sandbox)."
  ],
  "acceptance_criteria": [
    "No test file references 'azure_search' or 'azure.search' in imports or mock paths",
    "pytest apps/backend/ runs without import errors (some tests may skip due to missing DB)",
    "ruff check apps/backend/tests/ passes"
  ],
  "verification": [
    "Run: grep -r 'azure_search\\|azure.search\\|AzureSearch' /projects/sandbox/-kalukaliya/apps/backend/tests/ - should return empty",
    "Run: cd /projects/sandbox/-kalukaliya/apps/backend && APP_ENV=test JWT_SECRET=test-secret-at-least-32-characters-long python -m pytest tests/ --tb=short -q 2>&1 | tail -20"
  ],
  "blocked_reason": null,
  "findings": ""
}
