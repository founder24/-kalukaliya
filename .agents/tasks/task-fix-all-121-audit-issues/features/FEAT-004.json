{
  "id": "FEAT-004",
  "type": "fix",
  "description": "Fix all remaining Low severity issues across all categories (HF-007/008/010/012/014/019/026/030/036-039/044/046-048/050/054-055/059-063/065/067-068/070/073-074/076/079/082-085/089-090/093-098/104-105/108-112)",
  "status": "in_progress",
  "steps": [
    "HF-007: In apps/frontend/src/hooks/useTokenManager.js, add a guard `typeof window !== 'undefined'` before sessionStorage access (also fixes HF-050). Wrap sessionStorage calls in try-catch.",
    "HF-008: In apps/backend/app/api/v1/auth.py, add a comment documenting the concurrent session limitation and noting that a Redis set per user could enforce max 5 sessions.",
    "HF-010: In apps/backend/app/api/v1/admin.py, change cookie path from '/api/v1' to '/api/' for broader coverage. Search for path='/api/v1' in set_cookie call.",
    "HF-012: In apps/backend/app/core/token_budget.py, add a module-level docstring comment noting: 'Limitation: Uses character-based heuristic. For precise counting, integrate tiktoken.'",
    "HF-014: In apps/backend/app/services/chat_service.py in _invalidate_history_cache, add a comment noting the hardcoded values limitation. The approach is acceptable since the only caller uses max_turns=5.",
    "HF-019: In apps/backend/app/services/chat_service.py, add the 'data: ' prefix to the sentinel yield: change `yield json.dumps({...})` to `yield f\"data: {json.dumps({...})}\\n\\n\"` and update the consumer in chat.py to still detect __internal_complete from the data-prefixed format.",
    "HF-026: Add a comment in apps/backend/app/api/deps/rate_limit.py documenting the double rate limiting architecture (edge hourly + backend monthly) and noting the intentional overlap.",
    "HF-030: In apps/backend/app/models/user.py, remove 'trialing' from the subscription_status Literal type since it is never used.",
    "HF-036: Add a comment in apps/backend/app/models/knowledge.py on the slug field noting that URL-safety validation should be added.",
    "HF-037: In apps/backend/app/models/knowledge.py, add a field_validator for slug that enforces URL-safe format: `re.match(r'^[a-z0-9][a-z0-9-]*[a-z0-9]$', v)` (allow existing data by only validating new entries).",
    "HF-038: In apps/backend/app/db/mongo.py in create_indexes, the TTL index for dead_letters already exists with expireAfterSeconds. Verify the chat_feedback TTL index also exists; if not, add: `await db.chat_feedback.create_index([('timestamp', 1)], expireAfterSeconds=30*24*60*60)`",
    "HF-039: Add a comment in frontend noting the language mismatch between backend default 'as' and frontend default 'en'. This is by design (frontend fetches user preference on load).",
    "HF-044: Add a comment in apps/edge/src/middleware/rate-limit.ts documenting the eventual consistency trade-off.",
    "HF-046: Add a deprecation notice at the top of apps/backend/app/core/rate_limiter.py (if it exists) warning against importing it.",
    "HF-047: Add a comment in apps/backend/app/api/v1/users.py noting that /me is protected by JWT auth and edge rate limiting covers it.",
    "HF-048: In apps/backend/app/api/v1/auth.py, change the 'unknown' fallback to include user-agent hash: `or hashlib.sha256((request.headers.get('User-Agent') or 'unknown').encode()).hexdigest()[:16]`",
    "HF-054: Add a comment in apps/frontend/src/utils/api.jsx noting that _authToken is used via authConfig() and the interceptor does not auto-inject it.",
    "HF-055: In apps/frontend/src/hooks/useTokenManager.js clearTokens, add: `try { delete axios?.defaults?.headers?.common?.['Authorization']; } catch {}`",
    "HF-059/060/061/062/063: Add comments in ChatPage.jsx noting these known limitations (scroll throttling, rehype-sanitize, no virtualization, loading state, unbounded Set). These are documented trade-offs.",
    "HF-065: In apps/backend/app/db/mongo.py, add `heartbeatFrequencyMS=10000` to the AsyncIOMotorClient constructor.",
    "HF-067: Add a comment in dead_letter.py noting the O(n) pagination limitation.",
    "HF-070: Add a comment in azure_search.py noting the cache is correctly tier-segregated.",
    "HF-073: Add a comment in resend_client.py noting the in-memory rate limiter is per-instance only.",
    "HF-074: Add a comment in the razorpay service client noting the 30s timeout concern.",
    "HF-076: Add a comment noting the 30s timeout in cloudflare_client.",
    "HF-079: Add a comment in resend_client.py noting the url_quote encoding contract.",
    "HF-082-085: In apps/backend/app/api/v1/chat.py image endpoint, add magic bytes validation for common image formats after reading the file.",
    "HF-089: Add a comment in dead_letter.py replay noting the lack of backoff.",
    "HF-090: Add a comment in redis.py noting this is a non-issue for HTTP-based Upstash.",
    "HF-093: In apps/backend/app/api/v1/chat.py in get_chat_history, call generate_title on chats that have None title before returning.",
    "HF-095: Add a comment in payments.py /history endpoint noting the raw document return.",
    "HF-096: In apps/backend/app/api/v1/chat.py in get_chat_messages, filter message dicts to only include user-facing fields: role, content, timestamp, model_used.",
    "HF-097: Document the plan alias in users.py with a clear comment.",
    "HF-098: In apps/backend/app/api/v1/chat.py in get_chat_history, change message_count to only count user+assistant messages.",
    "HF-104/105: Add comments noting these are properly handled or not actual issues.",
    "HF-108-112: Add comments documenting these low-priority config concerns."
  ],
  "acceptance_criteria": [
    "All modified files pass syntax checks",
    "sessionStorage access is guarded in useTokenManager.js",
    "trialing removed from subscription_status Literal",
    "Sentinel uses data: prefix in SSE format",
    "chat_feedback TTL index is created programmatically",
    "heartbeatFrequencyMS added to MongoDB connection"
  ],
  "verification": [
    "Run: cd /projects/sandbox/-kalukaliya/apps/backend && python -c \"import ast; import glob; [ast.parse(open(f).read()) for f in glob.glob('app/**/*.py', recursive=True)]\" to verify all Python files have valid syntax"
  ],
  "blocked_reason": null,
  "findings": ""
}
