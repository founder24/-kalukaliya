# Legacy Frontend Directory

> **This directory is retained for reference only.**

The active frontend codebase has moved to `/apps/frontend/`.

All new development, bug fixes, and feature work should be done in the
`/apps/frontend/` directory, which uses React + Vite and is part of the
pnpm workspace monorepo.

This legacy directory may be removed in a future cleanup.
