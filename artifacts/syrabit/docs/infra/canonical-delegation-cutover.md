# Canonical-Delegation Cutover Runbook (Task #559)

> **Status: LIVE — 2026-05-07.** Operators run this once when adopting the per-feature canonical map (`infra/four-cloud-delegation.md` §A) on a given environment. Each step is a binary acceptance check.
> **ADR:** [`docs/architecture/adr/0003-canonical-strict-specialist-delegation.md`](../../../docs/architecture/adr/0003-canonical-strict-specialist-delegation.md)
> **V4 lock:** `infra/v4-locked-architecture.md` §17.
> **Umbrella guard:** `artifacts/syrabit-backend/scripts/ci/check_canonical_delegation.py`.

The cutover swaps the legacy "weighted-pool plus dead-provider-bans" routing world for the "one canonical primary + one strict fallback per feature, enforced by a single umbrella guard" world. There is no traffic-shifting step — the per-feature map already mirrors what the dispatcher does on `main`. The cutover is mechanical: rename, gate, snapshot, document.

| # | Step | Acceptance check | Rollback |
|---|---|---|---|
| 1 | **Land the umbrella in dry-run.** Add `scripts/ci/check_canonical_delegation.py` and rewrite `scripts/check_dead_providers.py` as the shim. Run both locally and confirm parity with the previous guard run on the same commit. | `python artifacts/syrabit-backend/scripts/check_dead_providers.py` exits 0 with the same OK summary as before; `python artifacts/syrabit-backend/scripts/ci/check_canonical_delegation.py` exits 0. | Revert the umbrella file; `scripts/check_dead_providers.py` keeps its old logic. |
| 2 | **Migrate the chat-dispatch unit-tests.** Confirm `tests/test_provider_priority_locked.py` already pins the runtime chain shape (`_select_chat_primary` returns `[vertex, workers_ai_llama32_3b]` by default, flipped on ≤90 d runway, override knob honoured / negative). Add a snapshot assert against the new umbrella's `_check_chat_chains` if missing. | `cd artifacts/syrabit-backend && pytest tests/test_provider_priority_locked.py -q` is green. | n/a — tests-only. |
| 3 | **Verify static `PROVIDER_PRIORITY` membership.** Both `english_rag_chat` and `assamese_rag_chat` lists in `config.py` must equal the canonical sets. The umbrella's `_check_chat_chains` enforces this; run it directly. | `python -c "from scripts.ci.check_canonical_delegation import _check_chat_chains; assert not _check_chat_chains()"` from inside `artifacts/syrabit-backend/`. | Edit `config.py` back to the canonical lists. |
| 4 | **Wire the umbrella into the deploy workflow as a hard gate.** Add a `canonical_delegation_gate` job to `.github/workflows/azure-container-apps-deploy.yml` that runs before `budget_ceiling_gate`; make `budget_ceiling_gate` (and therefore `deploy`) depend on it. | Trigger `workflow_dispatch` with `mode=redeploy`; both gates run before any image build. Job log shows "Canonical-delegation guard OK — scanned N files." | Remove the `needs: canonical_delegation_gate` line; the deploy falls back to the previous single-gate flow. |
| 5 | **Decommission the legacy guard's call-sites.** Search the repo for direct calls to `check_dead_providers.py` (CI workflows, pytest fixtures, ops cron). Each surviving call is fine — they go through the shim — but new call-sites should target the umbrella directly. | `rg -n "check_dead_providers" .github artifacts/syrabit-backend/tests` shows only legacy entry-points (the shim itself + `tests/test_dead_providers_guard.py`). | n/a — additive. |
| 6 | **Rotate operator overrides to documented env-var names.** Every override knob in the canonical map (`CHAT_PRIMARY_OVERRIDE`, `RAG_EMBEDDING_PROVIDER_FORCE`, `EMBED_DEGRADED_MODE`, `EMAIL_PROVIDER`, `EMAIL_FALLBACK`) must be present in `replit.md` "Required env vars" or in the row's notes column in §A. Audit the ACA env-var set against this list. | Spot-check Azure portal → Container Apps → `syrabit-backend` → Environment variables: every override knob from §A is either present (with documented value) or absent (default behaviour). | Reset any drifted env-var to the documented default; restart the revision. |
| 7 | **Confirm the voice paywall.** `/tts`, `/stt`, `/voice/voice` must each sit behind `Depends(require_paid_plan)` (Task #549). The umbrella's `_check_voice_paywall` mirrors the assertion in `scripts/check_budget_ceiling.py` so a single-guard skip cannot smuggle the paywall removal. | `python -c "from scripts.ci.check_canonical_delegation import _check_voice_paywall; assert not _check_voice_paywall()"` from `artifacts/syrabit-backend/`; live probe `curl -i $API/api/voice/tts` as a free user returns `402`. | Re-add `Depends(require_paid_plan)` to the offending route; redeploy. |
| 8 | **Confirm `formatted_by` audit field.** All chapter / translation docs polished after Task #494 carry `formatted_by ∈ {vertex, workers_ai_llama33_70b, passthrough}`. Spot-check the admin health panel (`/admin/health/content_formatter`) — non-zero counts for the last 256 invocations across the three buckets prove the dispatcher is the only path. | Admin panel shows a non-empty rolling breakdown; Mongo `db.chapters.findOne({}, {formatted_by: 1})` returns one of the three values for any post-#494 doc. | n/a — read-only verification. |
| 9 | **Tag the canonical-map version into the V4 changelog.** Append a §17 amendment in `infra/v4-locked-architecture.md` referencing the ADR + this runbook + the umbrella guard. Bump the V4 doc's "last spec-clarity pass" date in the header. | `git diff infra/v4-locked-architecture.md` shows a new §17 amendment block citing ADR-0003 and this runbook. | Revert the §17 block; the rest of the cutover keeps working but the V4 doc no longer matches the canonical map. |
| 10 | **Snapshot the cost-share post-cutover and refresh `replit.md`.** Recompute the headline cost-share from the credit-runway memo (Task #550) once the new map is in effect. Update the "Cost split" line in `replit.md` to the snapshot value (currently **40 % CF / 30 % GCP / 15 % Az / 10 % AWS / 5 % other**) and add a "Canonical specialist delegation" subsection pointing at §A. | `git diff replit.md` shows the cost-split line plus the new subsection. The "Architecture decisions" block now lists Task #559 alongside #549 / #550. | Revert the `replit.md` snippet; the canonical map still binds, but operators reading `replit.md` will see the old percentages until the next refresh. |

## Drill cadence

- Run steps 1 + 3 + 4 + 7 on **every PR** that touches `infra/`, `config.py`, `cost_caps.py`, `routes/voice.py`, or any `scripts/ci/*` file. The deploy workflow does steps 4 + 7 automatically.
- Run the full 10 steps quarterly alongside the V4 §8 DR drill so the canonical map stays in sync with the runway memo (Task #550 quarterly review cadence).

## TODO (when Tasks #557 / #558 ship)

- Step 11 (added by #557): flip the SES + self-hosted-web-push bans from TODO to active in the umbrella (`TODO_557_PATTERN`); update `replit.md` "Required env vars" to add `WEB_PUSH_VAPID_PRIVATE_KEY` and remove SendGrid / FCM keys.
- Step 12 (added by #558): flip the observability ban from TODO to active in the umbrella (`TODO_558_PATTERN`); confirm `OTEL_TRACES_EXPORTER=googlecloud` is the only value present in the ACA env-var set.
