# services/cron-jobs/requirements.txt
#
# Phase 4 — Cron port (Task #332).
#
# Runtime deps for the Azure Container Apps Jobs runner image. Kept
# deliberately narrow — the legacy backend wheel (staged into
# `_backend/` by the release pipeline) carries its own deeper
# dependency tree; only the libraries the runner script itself needs
# (signal handling, Mongo heartbeat write, OTEL → App Insights
# exporter) are pinned here.
#
# Pins follow the legacy backend's pyproject.toml so a single dep-
# bump PR can update both files in lockstep. CI fails if these drift.

# Cron-runner extras layered on top of the legacy backend
# requirements (installed first by the Dockerfile). Keep this list
# minimal — anything the backend already ships does not belong here.
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
azure-monitor-opentelemetry-exporter>=1.0.0b30
azure-identity>=1.17.0   # Read by routes/admin_azure_cron.py for ARM polling
orjson>=3.10.7
