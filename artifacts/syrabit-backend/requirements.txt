fastapi==0.136.1
uvicorn[standard]==0.25.0
gunicorn==25.3.0
pydantic==2.12.5
python-multipart==0.0.27
python-dotenv==1.2.2
email-validator==2.3.0

pymongo==4.17.0
motor==3.7.1
asyncpg==0.31.0
supabase==2.28.0
upstash-redis>=1.3.0  # Serverless Redis REST client — primary L2 cache + atomic rate limiting
redis>=5.0.1
fakeredis>=2.20  # Task #631 — in-memory Redis for ai_cache integration tests
lupa>=2.8  # Task #765 — enables fakeredis Lua/EVAL execution for atomic credit deduct test

google-auth==2.49.1
google-auth-oauthlib==1.3.0
google-api-core==2.30.0

httpx==0.28.1
aiohttp==3.13.5

# Task #332 — AWS SQS producer + Azure ARM cron-health proxy.
boto3>=1.35.0  # Used by sqs_fanout.py + routes/admin_aws_infra.py
# Task #347 — OpenAI SDK retained as a generic OpenAI-compatible HTTP
# client (AsyncOpenAI) used to talk to Azure OpenAI / Workers AI /
# Cloudflare AI Gateway endpoints. The OpenAI provider itself is
# decommissioned (no calls to api.openai.com) — the SDK class is kept
# only for its base_url/api_key plumbing and typed exception classes.
openai>=1.51.0,<3.0.0
azure-identity>=1.17.0  # Used by routes/admin_azure_cron.py for ARM token
h2==4.3.0
httptools==0.7.1
uvloop==0.22.1

PyJWT==2.12.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==47.0.0

cachetools==6.2.6
filelock==3.25.2
numpy==2.4.4
Pillow==12.2.0
lxml==6.1.0
mistune==3.2.0
nh3==0.3.5

razorpay==2.0.1
boto3>=1.34.0  # R2 Object Storage — S3-compatible API for Cloudflare R2
duckduckgo_search==8.1.1
dnspython==2.7.0

pywebpush==2.3.0
py-vapid==1.9.4
pypdf>=6.10.2
cloudflare>=4.3.0

# ── Observability (Task #610) — OpenTelemetry distributed tracing ──────────
# All optional at runtime: tracing.py degrades to no-op if these are absent.
# OpenTelemetry — pinned to a known-good combination (Task #634).
# API/SDK 1.27.0 ↔ instrumentation 0.48b0 ↔ gcp-trace exporter 1.7.0.
# These are paired releases — bump them together or the instrumentation
# stack will fail to import. Verified resolvable via `pip install --dry-run`.
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-httpx==0.48b0
# Task #333 — observability rewire. Replaced the GCP Cloud Trace
# exporter with two parallel OTLP-capable exporters: Azure Monitor
# (App Insights, primary unified APM sink) and the standard OTLP/HTTP
# exporter (used to ship to Axiom). See `tracing.py`.
azure-monitor-opentelemetry-exporter==1.0.0b30
opentelemetry-exporter-otlp-proto-http==1.27.0
pydantic-settings>=2.0.0
