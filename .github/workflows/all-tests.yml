name: all-tests

#
# Supply-chain hardening: every `uses:` reference in this file is pinned to a
# full 40-char commit SHA (with a trailing `# vX.Y.Z` tag comment for
# readability). Float tags like `actions/checkout@v4` are forbidden because a
# compromise of the upstream tag — as happened with tj-actions/changed-files in
# 2025 — would let an attacker silently exfiltrate this repo's secrets on the
# next push. See https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions#using-third-party-actions
# To upgrade an action: look up the SHA for the new tag (e.g.
# `gh api repos/actions/checkout/git/refs/tags/v4.3.0 --jq .object.sha`) and
# update both the SHA and the trailing tag comment.
#
# Least-privilege scopes: every job declares an explicit top-level
# `permissions:` block granting only the `GITHUB_TOKEN` scopes it actually
# needs — read-only by default.
#
# Wire-up CI gate for all 75 tests (Task #2).
#
# Runs the complete test suite — Vitest unit/component tests AND the full
# Playwright E2E suite — on every push and pull request to `main`, with no
# path filtering. A single red test blocks the merge.
#
# Why a separate workflow rather than extending frontend-tests.yml?
# - `frontend-tests.yml` intentionally stays lightweight (vitest + build only).
# - Playwright requires installing Chromium and a running dev server, which
#   adds ~4-6 min. Keeping them separate means a vitest-only PR doesn't pay
#   the Playwright overhead, while this gate still fires on every change.
# - Both workflows are listed as required checks in branch-protection so
#   neither can be skipped.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Read-only token: checks out the repo, runs tests, uploads a Playwright
# report artifact on failure. Does not write back to the repo.
permissions:
  contents: read

jobs:
  backend-import-smoke:
    name: Backend import smoke test
    runs-on: ubuntu-latest
    # Fast gate: verifies every route module imports cleanly (no missing
    # dependency, no circular import, no syntax error) before the slower
    # Vitest and Playwright jobs start.  Mirrors the manual check documented
    # in replit.md: `cd artifacts/syrabit-backend && python -c "import server"`.
    timeout-minutes: 5

    defaults:
      run:
        working-directory: artifacts/syrabit-backend

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          submodules: false
          set-safe-directory: false

      - name: Set up Python
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: artifacts/syrabit-backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Import server (validates all route modules load)
        # Provide the minimum env vars the module-level code reads at import
        # time (JWT_SECRET length guard, DB_NAME, MONGODB_URL).  No real
        # secrets are needed — the smoke test never makes network calls.
        env:
          MONGODB_URL: "mongodb://localhost:27017/test"
          DB_NAME: "syrabit_test"
          JWT_SECRET: "ci-test-secret-not-real-padded-to-64-chars-of-throwaway-entropy-aaaaaa"
          ADMIN_JWT_SECRET: "ci-admin-test-secret-not-real-padded-to-64-chars-of-throwaway-entropy-bbbbbb"
        run: python -c "import server; print('server import OK')"

  vitest:
    name: Vitest unit tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          submodules: false
          set-safe-directory: false

      - name: Set up pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Run edge worker unit tests (vitest)
        run: pnpm --filter ./workers/edge-proxy run test --run

      - name: Run syrabit web unit tests (vitest)
        run: pnpm --filter @workspace/syrabit run test

  playwright:
    name: Playwright E2E tests
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          submodules: false
          set-safe-directory: false

      - name: Set up pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8

      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Install Playwright Chromium + system deps
        run: pnpm --filter @workspace/syrabit exec playwright install --with-deps chromium

      - name: Build syrabit app (pre-compile for vite preview)
        # build:client = bare `vite build` — skips env-var gating, SSR, prerender,
        # and verify scripts that require a real backend.  The resulting dist/ is
        # served by `vite preview` during the Playwright run so every module is
        # already compiled and page loads stay well under the 10 s assertion timeout.
        #
        # IMPORTANT: do NOT set VITE_BACKEND_URL here.  When it is set to an
        # absolute URL (e.g. http://localhost:8080), it gets baked into the
        # production bundle.  Every API call then goes to a different origin than
        # the vite-preview server (127.0.0.1:4317), triggering the browser's
        # CORS + withCredentials check — which blocks the response even though
        # Playwright's page.route() interceptor fulfills it.  Without the env
        # var, VITE_BACKEND_URL defaults to '' so API_BASE resolves to '/api'
        # (same-origin relative path).  vite preview serves the SPA on 4317;
        # Playwright's '**/api/**' glob matches the resolved same-origin URLs
        # and intercepts them correctly, letting AdminGuard pass.
        run: pnpm --filter @workspace/syrabit run build:client

      - name: Run full Playwright E2E suite
        run: pnpm --filter @workspace/syrabit run test:e2e
        env:
          CI: "true"

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: all-tests-playwright-report
          path: |
            artifacts/syrabit/playwright-report/
            artifacts/syrabit/test-results/
          if-no-files-found: ignore
          retention-days: 7
