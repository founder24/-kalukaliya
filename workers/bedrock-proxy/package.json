{
  "name": "bedrock-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — SigV4 proxy for AWS Bedrock services (Polly, Transcribe, Translate) not covered by the CF AI Gateway aws-bedrock BYOK slug.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.78.10"
  }
}
