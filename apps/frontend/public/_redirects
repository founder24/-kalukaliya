# Trailing-slash normalisation — strip trailing slash from all SPA routes
# so browsers and crawlers always land on the canonical (no-slash) URL.
# CF Pages serves these before the Worker runs.
/library/    /library    308
/pricing/    /pricing    308
/login/      /login      308
/signup/     /signup     308
/chat/       /chat       308
/browser/    /browser    308
/about/      /about      308
/terms/      /terms      308
/privacy/    /privacy    308
/dashboard/  /dashboard  308
/settings/   /settings   308
