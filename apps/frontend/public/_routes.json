{
  "version": 1,
  "include": ["/*"],
  "_comment_xml_routes": "Sitemap XML files are pre-generated static CDN assets. Feed and LLM-index paths must remain outside exclude so _worker.js can proxy them to the native API Worker.",
  "_comment_ads_txt": "ads.txt MUST be in exclude — it is a plain-text static file served directly from the CDN. Without this, the worker intercepts it, falls back to PAGES_ORIGIN, which re-enters the same Pages deployment creating an infinite-proxy loop that always returns the SPA HTML instead of the AdSense verification line.",
  "exclude": [
    "/assets/*",
    "/fonts/*",
    "/icons/*",
    "/favicon.ico",
    "/favicon.svg",
    "/logo.webp",
    "/logo-56.webp",
    "/logo-144.webp",
    "/manifest.json",
    "/sw.js",
    "/opengraph.jpg",
    "/cover-bg.webp",
    "/yoga-cover-notext.webp",
    "/apple-touch-icon.png",
    "/offline.html",
    "/googlefbe0a804ad7e5fdd.html",
    "/_headers",
    "/ads.txt",
    "/87babfeb017d443f900a0eb4595466b3.txt",
    "/sitemap-index.xml",
    "/sitemap-static.xml",
    "/sitemap-subjects.xml",
    "/sitemap-chapters.xml",
    "/sitemap-topics.xml",
    "/static/*",
    "/.well-known/*"
  ]
}
