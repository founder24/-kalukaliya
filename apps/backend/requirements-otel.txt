# OpenTelemetry packages for distributed tracing (Phase 5)
# Install with: pip install -r requirements-otel.txt
#
# These are OPTIONAL — the app runs fine without them.
# When not installed, telemetry.py gracefully falls back to no-op.

opentelemetry-api>=1.42.1
opentelemetry-sdk>=1.42.1
opentelemetry-exporter-otlp>=1.42.1
opentelemetry-instrumentation-fastapi>=0.46b0
opentelemetry-instrumentation-httpx>=0.46b0
opentelemetry-instrumentation-pymongo>=0.46b0
