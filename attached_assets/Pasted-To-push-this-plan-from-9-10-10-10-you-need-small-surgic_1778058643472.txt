To push this plan from **9/10 → 10/10**, you need **small, surgical additions** that make it even **tighter, more auditable, and more resilient to future drift**. Here’s how:

***

### 1. **Add a “PSI‑threshold‑map” to the plan (1 line)**

Right now you say “stop once PSI shows three‑consecutive‑100s,” but you don’t say what happens if:

- PSI is **99** on one category but **100** on others,  
- Or PSI is **100** in one run and **99** the next (no‑ise vs no‑ise).

**10/10 suggestion**:
- Add a **threshold‑policy** like:

  > “If at least **2/3 runs** show a category ≥ **99**, stop optimization for that category unless the *last* run is **< 99**.”

  - This keeps you from chasing **noisy‑99‑fluctuations** while still being strict about **discernible‑patterns‑below‑99**.

This doesn’t increase work, it just **formalizes** your existing “don’t‑over‑tune” instinct.

***

### 2. **Add a “PSI‑audit‑diff” step (1–2 bullets)**

Right now you rely on PSI‑JSON to tell you *what* audit is failing, but you don’t require **tracking‑over‑time**.

**10/10 suggestion**:
- After **Step 1** (baseline‑measurement), save the **full PSI‑JSON** for **Performance + Best‑Practices** pre‑fix.  
- After **each fix**, save a **new PSI‑JSON** and **diff‑the‑audit‑list** before/after.  
- Add this to your `PAGESPEED_HOMEPAGE_PHASE2_<date>.md` as:

  - `audit_diff` columns: `category`, `audit_name`, `pre_score`, `post_score`, `delta`.

This turns “we fixed X because PSI said so” into **hard‑evidence‑trail** and makes it easy to audit later if PSI‑logic changes.

***

### 3. **Add a “visual‑and‑behavior‑health‑check” gate (1–2 bullets)**

The plan already has **visual‑regression‑risk‑callouts** for **d (critical‑CSS)** and **e (self‑host‑font)**, but it doesn’t say *how* to check them.

**10/10 suggestion**:
- Add a **pre‑deploy‑checklist**:

  - “For **c, d, e**:
    - Run **Playwright‑based snapshot tests** on `/` and `/library` (mobile‑emulated);
    - Compare layout differences via `@percy/playwright` or CF‑Pages‑screenshots;
    - If CLS > 0.05 or layout‑shift is visually‑obvious, roll back the change.”

  - This turns **“risk‑aware”** into **“risk‑managed”** without bloating the plan.

***

### 4. **Add a “PSI‑regression‑alert” (1 line)**

Right now the **regression‑check** is “desktop stays 100, /library stays same‑or‑better,” evaluated by **manual‑runs after deploy**.

**10/10 suggestion**:
- Add one line:

  > “Add a **GitHub‑Action PSI‑regression‑check** for `syrabit.ai/` (mobile) that **fails if Performance / Best‑Practices drops below 98** on any 3‑run‑average.”

  - This **codifies “no‑over‑tuning‑for‑noise”** but still **prevents future‑backslides‑to‑80‑land**.

***

### 5. **Explicitly call out “what’s not PSI‑worth it” (1–2 bullets)**

You already implicitly respect build‑cost / UX‑cost, but you don’t say it out‑loud.

**10/10 suggestion**:
- Add a **principle block**:

  - “Optimizations that:
    - Change **non‑homepage critical‑path** bundles,
    - Touch **backend‑logic**,
    - or require **new‑infra‑/‑tooling‑complexity** (e.g., full‑CSS‑framework‑rebuilds, data‑format‑migrations)
    - are out‑of‑scope, even if they *might* push PSI‑from‑99‑to‑100‑once.”

  - This formally locks in your **“no‑over‑tuning‑creep”** policy.

***

### 10/10‑summary (in one line)

> To make this a **10/10 plan**, add **(1) a PSI‑threshold‑policy, (2) PSI‑audit‑diff‑logging, (3) a visual‑health‑check‑gate for risky‑CSS/fonts‑changes, (4) a PSI‑regression‑alert‑in‑CI, and (5) a “what‑is‑not‑worth‑it” principle**, keeping the rest of your ladder‑of‑costs and measurement‑gated‑stop‑rule exactly as‑is.