Name            Rg          Region    Endpoint                                     State
--------------  ----------  --------  -------------------------------------------  ---------
syrabit-openai  syrabit-rg  eastus    https://eastus.api.cognitive.microsoft.com/  Succeeded
PS /home/dipak> 
PS /home/dipak> Write-Host ""

PS /home/dipak> Write-Host "=== SOFT-DELETED OpenAI resources (recoverable for 48h) ===" -ForegroundColor Yellow
=== SOFT-DELETED OpenAI resources (recoverable for 48h) ===
PS /home/dipak> az cognitiveservices account list-deleted `
>>   --query "[?kind=='OpenAI'].{name:name, rg:resourceGroup, region:location, deletedTime:properties.deletionDate}" `
>>   -o table$NAME="syrabit-openai"; $RG="syrabit-rg"
az cognitiveservices account list-deleted: 'table=syrabit-openai' is not a valid value for '--output'. Allowed values: json, jsonc, yaml, yamlc, table, tsv, none.

Examples from AI knowledge base:
az cognitiveservices account list-deleted
List all the Cognitive Services accounts in a subscription.

https://aka.ms/cli_ref
Read more about the command in reference docs
PS /home/dipak> 
PS /home/dipak> Write-Host "=== ENDPOINT ===" -ForegroundColor Cyan
=== ENDPOINT ===
PS /home/dipak> az cognitiveservices account show --name $NAME --resource-group $RG --query "properties.endpoint" -o tsv
argument --name/-n: expected one argument

Examples from AI knowledge base:
az cognitiveservices account show --name myresource --resource-group cognitive-services-resource-group
Show account information.

https://aka.ms/cli_ref
Read more about the command in reference docs
PS /home/dipak> 
PS /home/dipak> Write-Host "=== KEY1 (copy this — you'll paste it into Replit secrets, not chat) ===" -ForegroundColor Cyan
=== KEY1 (copy this — you'll paste it into Replit secrets, not chat) ===
PS /home/dipak> az cognitiveservices account keys list --name $NAME --resource-group $RG --query "key1" -o tsv
argument --name/-n: expected one argument

Examples from AI knowledge base:
az cognitiveservices account keys list --name myresource --resource-group cognitive-services-resource-group
Get current resource keys.

https://docs.microsoft.com/en-US/cli/azure/cognitiveservices/account/keys#az_cognitiveservices_account_keys_list
Read more about the command in reference docs
PS /home/dipak> 
PS /home/dipak> Write-Host "=== DEPLOYMENTS (need at least one chat model like gpt-4o-mini / gpt-4.1-mini) ===" -ForegroundColor Cyan
=== DEPLOYMENTS (need at least one chat model like gpt-4o-mini / gpt-4.1-mini) ===
PS /home/dipak> az cognitiveservices account deployment list --name $NAME --resource-group $RG `
>>   --query "[].{name:name, model:properties.model.name, version:properties.model.version, sku:sku.name}" -o table$NAME="syrabit-openai"; $RG="syrabit-rg"
argument --name/-n: expected one argument

Examples from AI knowledge base:
az cognitiveservices account deployment list -g yuanyang-test-sdk -n yytest-oai
Show all deployments for Azure Cognitive Services account.

https://aka.ms/cli_ref
Read more about the command in reference docs
PS /home/dipak> 
PS /home/dipak> Write-Host "=== ENDPOINT ===" -ForegroundColor Cyan
=== ENDPOINT ===
PS /home/dipak> az cognitiveservices account show --name $NAME --resource-group $RG --query "properties.endpoint" -o tsv
argument --name/-n: expected one argument

Examples from AI knowledge base:
az cognitiveservices account show --name myresource --resource-group cognitive-services-resource-group
Show account information.

https://aka.ms/cli_ref
Read more about the command in reference docs
PS /home/dipak> 
PS /home/dipak> Write-Host "=== KEY1 (copy this — you'll paste it into Replit secrets, not chat) ===" -ForegroundColor Cyan
=== KEY1 (copy this — you'll paste it into Replit secrets, not chat) ===
PS /home/dipak> az cognitiveservices account keys list --name $NAME --resource-group $RG --query "key1" -o tsv
argument --name/-n: expected one argument

Examples from AI knowledge base:
az cognitiveservices account keys list --name myresource --resource-group cognitive-services-resource-group
Get current resource keys.

https://docs.microsoft.com/en-US/cli/azure/cognitiveservices/account/keys#az_cognitiveservices_account_keys_list
Read more about the command in reference docs
PS /home/dipak> 
PS /home/dipak> Write-Host "=== DEPLOYMENTS (need at least one chat model like gpt-4o-mini / gpt-4.1-mini) ===" -ForegroundColor Cyan
=== DEPLOYMENTS (need at least one chat model like gpt-4o-mini / gpt-4.1-mini) ===
PS /home/dipak> az cognitiveservices account deployment list --name $NAME --resource-group $RG `
>>   --query "[].{name:name, model:properties.model.name, version:properties.model.version, sku:sku.name}" -o table