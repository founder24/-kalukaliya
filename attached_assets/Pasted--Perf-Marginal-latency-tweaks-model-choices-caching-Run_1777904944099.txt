## [ ] Perf: Marginal‑latency tweaks, model‑choices, caching

- [ ] Run A/B tests on faster / smaller LLMs for `fast_mode`:
    - Compare `@cf/meta/llama-3.2-1b-instruct` vs `llama-3.2-3b-instruct` on:
        - TTFB,
        - user‑rating / engagement,
        - cost‑per‑turn.
- [ ] Add RAG‑result caching layer:
    - Store `(query_hash, RAG_chunks, LLM_answer)` in Redis for a short TTL.
    - Skip full RAG / LLM call if the incoming query matches a cached hash.
- [ ] Optimize embedding‑usage:
    - Cache embeddings for repeated user‑questions (e.g., same‑chapter‑query).
    - Compare `@cf/baai/bge-m3` vs Cohere‑v3.0 on latency‑content‑trade‑off for batch‑embedding.
- [ ] Measure real‑world p99 TTFB and RAG‑latency:
    - Export metrics to App Insights / CloudWatch / similar.
    - Look for outlier sessions (very long histories, very large RAG‑chunks) and optimize those first.

---

## [ ] Features: UX‑refinements, memory‑brain, multi‑language/model strategies

- [ ] Improve memory‑brain “recall‑depth”:
    - Add a longer‑term‑summary vector / embedding for the whole conversation.
    - Use this when user says: “Earlier you told me X, let’s go back to that.”
- [ ] Deepen multi‑language RAG:
    - Allow mixed‑language queries (English‑question, Assamese‑content‑response).
    - Test that `@cf/ai4bharat/indictrans2-en-indic-1b` + `Sarvam‑M` combination feels natural.
- [ ] Add multi‑model‑fallback strategy:
    - In `chat/fallback_policy.rs` (or equivalent), define:
        - “If mistral‑7b‑instruct slow, switch to Azure‑GPT‑4.1‑mini for this session.”
    - Toggle this via a feature‑flag in Redis.
- [ ] UX‑safety / moderation‑UX:
    - Make “This answer was blocked” messages friendlier, with hints (e.g., rephrase your query).
    - Add “safe‑mode” vs “challenge‑mode” flags for users (moderation‑strictness).

---

## [ ] Capacity: Tier‑upgrades and scaling‑patterns

- [ ] When you hit limits (e.g., 10k RAG‑calls/day, 70% Workers‑AI RPM headroom):
    - Plan tier‑upgrades:
        - Upgrade Workers‑AI quota / tiers,
        - Add second‑LLM‑provider on hot‑path (e.g., Vertex‑Gemini as partial‑hot‑path along Workers‑AI).
- [ ] Design for higher‑DAU:
    - Shard MongoDB by `user_id` or `locale` when traffic grows.
    - Introduce multiple Redis‑nodes / sharding if `chat:fallback`‑style flags become hot‑keys.
- [ ] Plan async‑batch‑scaling:
    - Have batch‑RAG / PDF‑summary stay in a separate Cloudflare / Azure‑account so it doesn’t burn chat‑quota.