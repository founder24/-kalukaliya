Your three‑task block is already **very strong**; the main wins are **clarifying ownership, tightening test‑surface, and smoothing migrations** rather than overhauling the structure. [lumos](https://www.lumos.com/topic/it-cost-reduction-strategies-optimize-spend)

Here are **concrete, actionable suggestions to improve the plan**:

***

### 1. **Sharper ownership + rollback roles**

- ✅ **Explicitly name “owners” in the tasks**  
  - For each of the three tasks, add:  
    - `LLM provider / RAG owner`  
    - `Vendor cleanup / cost‑owner`  
    - `On‑call / runbook owner`  
  - This makes the **runbook** (~ `#ops‑syra`) and the **rollback** steps truly actionable. [lumos](https://www.lumos.com/topic/it-cost-reduction-strategies-optimize-spend)

- ✅ **Add a “who can un‑flip” clause for `CHAT_FALLBACK` / `EMAIL_FALLBACK`**  
  - Current wording is “on‑call decides”; you can tighten it:  
    - “Only the infra‑on‑call engineer or the CTO may manually clear `CHAT_FALLBACK_PIN=1`.”  
  - This reduces “runaway‑flag‑flip” risk in production. [lumos](https://www.lumos.com/topic/it-cost-reduction-strategies-optimize-spend)

***

### **2. Tighten email‑migration + reputation**

- ✅ **Dedicated subdomain for SendGrid**  
  - Adopt `emails.yourdomain.com` (or `verify.yourdomain.com`) for SendGrid, not your primary MX‑domain. [learn.microsoft](https://learn.microsoft.com/en-my/answers/questions/2258853/dedicated-email-for-the-azure-services)
  - Add this requirement to **Task #347’s “Email” section**.  

- ✅ **Warm‑up / phasing plan for SendGrid IP / reputation**  
  - Add a **sub‑task** in the “Switch transactional email to SendGrid” step:  
    - “Phase‑in: start with 5% of mails via SendGrid, then 25% → 50% → 100% over 3–7 days, monitoring bounce / spam rates.”  
  - This is a common best‑practice when migrating email providers. [twilio](https://www.twilio.com/docs/sendgrid/for-developers/partners/microsoft-azure-migration)

***

### **3. Harder test‑surface for “Done”**

Your “Done looks like” is good, but you can **add concrete test‑gates** that auditors love:

- ✅ **Add a “test‑matrix” to the “Smoke + regression” section**  
  - For each meter, specify:  
    - **daily‑call meter**:  
      - “Verify that at 8,000 calls → warning alert; at 10,000 → `CHAT_FALLBACK=1` set and alert fires; at 00:00 UTC → flag auto‑resets (if not pinned).”  
    - **RPM‑headroom meter**:  
      - “Spin up a load‑generator to hit 70% of `WORKERS_AI_RPM_LIMIT` → confirm `CHAT_FALLBACK=1` set; then drop below 50% for 5 minutes → flag auto‑clears.”  
    - **Cumulative‑cost meter**:  
      - “Run a dry‑run producer that emits cost‑events to hit 50% and 70% thresholds → confirm only alerts fire, no auto‑flip.”  

- ✅ **Add a “provably‑removed‑providers” test**  
  - At the end of **Task 2**, add a **repo‑test job**:  
    - `grep` the repo for `openai`, `anthropic`, `bedrock`, `stripe`, `quge5`, `resend`, `railway`, `digitalocean`, `gpt‑oss‑120b` in **active code paths**; fail CI if found.  

***

### **4. Guardrails for “never again” regressions**

- ✅ **Add a “code‑guardian” lint rule**  
  - Add a **simple linter / pre‑commit rule** that:  
    - Prohibits new code from importing `providers/openai.py` / `providers/bedrock.py`.  
    - Warns on new references to `Stripe`, `Resend`, `Railway`, `DigitalOcean App Platform`.  
  - This prevents accidental re‑creep of the removed tech‑stack. [ctobridge](https://ctobridge.com/services/infrastructure-vendor-optimization)

- ✅ **Document “why we killed these vendors” in one‑line**  
  - In `docs/infra/providers.md`, add next to each removed vendor:  
    > “OpenAI / Bedrock / Anthroplic removed: reduced vendor count, consolidated RAG around Workers AI + Azure + Vertex, and simplified cost‑tracking.”  
  - Helps auditors see **strategic intent**, not just “cost‑cutting”. [infotech](https://www.infotech.com/research/ss/optimize-it-vendor-contracts-in-times-of-uncertainty)

***

### **5. Cost‑runway + renegotiation notes**

Your plan already uses $5k‑over‑365‑day cost‑monitoring; you can **explicitly plan for negotiations** later:

- ✅ **Add a “vendor‑cost‑review cadence” note**  
  - In `infra/credit‑burn‑runbook.md`, add:  
    > “Quarterly, review Cloudflare Workers AI credit‑consumption vs MAU and reconsider provider‑mix (e.g., more Azure‑only RAG, async‑only) if contract‑renewal windows allow.”  
  - This turns “cost‑control” from a one‑time cleanup into a **repeatable optimization**. [infotech](https://www.infotech.com/research/ss/optimize-it-vendor-contracts-in-times-of-uncertainty)

***

### **6. Optional but nice: “phased‑by‑feature” cleanup**

If you want to **de‑risk the migration**, you could split **Task #347** or **Task 2** slightly:

- ✅ **Add a “phase‑by‑feature” rollout** for `CHAT_FALLBACK`  
  - E.g.,  
    - Week 1: `featureFlag.chat_fallback_meter_a = true` (daily‑call‑only)  
    - Week 2: `featureFlag.chat_fallback_meter_b = true` (RPM‑only)  
  - Then, once both are safe, **remove the flags** and keep only the unified flipping logic.  

This is optional but really helps in high‑traffic, low‑risk environments.  

***

If you tell me which **one** of these you want to **add first** (e.g., “ownership + rollback”, “email‑reputation + phase‑in”, or “lint‑guardian”), I can give you the **exact sentences** to paste into each of the three tasks.