Roadmap — basic → advanced
Each wave is sequenced so the cheap, low-risk wins land first and unblock the harder ones. "Mine" = I can build it; "Yours" = needs your decision/access/calendar.

Wave 0 — Dev hygiene (1–2 hrs total, zero user-visible risk)
Cleans the boot logs and CI so the next waves run on a quiet baseline.

Order	#	What	Owner	Why first
0.1	#23	Silence the Cloudflare AI Gateway WARN on every backend boot	Mine	Makes log triage in later waves trustworthy
0.2	#24	Frontend dev build stops requiring a prod env var	Mine	Unblocks anyone running pnpm build locally
0.3	#25	CI guard blocking accidental Bedrock wiring into chat/voice	Mine	Cheap insurance before we touch chat router again
0.4	#26	CI guard for the $100 monthly cap	Mine	Pure CI add; protects every later wave
Exit criteria: clean dev_health run, pnpm build works with a fresh checkout, two new CI jobs green.

Wave 1 — Finish the smart router (closes #37 family)
Builds directly on the merged Task #40 work.

Order	#	What	Owner
1.1	#41 (IN_PROGRESS)	Make fail-loud 503 turns carry route_trace so the QA badge renders even on errors	Mine
1.2	#42 (PROPOSED)	One-click "router log" panel in dev for failed chat turns (collapsible JSON of the full RouteDecision + retrieval state)	Mine — depends on 1.1
1.3	#38	Smart router actually skips Pinecone on casual chats (today the early-cache path bypasses retrieval but the router doesn't gate it explicitly)	Mine
1.4	#39	Replace the confidence proxy with the real topic-match score from RRF	Mine — depends on 1.3
1.5	#37	Mark parent closed once 1.1–1.4 are merged + Plan A canonical e2e green	Mine
Exit criteria: Plan A 3-turn Playwright test (casual → English RAG → Assamese RAG) green in dev and dev preview, with badge visible on every turn including errors.

Wave 2 — Turn on the cache shortcut for real users
This is where the $$ savings start. Has to come after Wave 1 because it depends on the router skipping Pinecone correctly.

Order	#	What	Owner
2.1	#17	Sync the bilingual topic dictionary from the syllabus DB (today it's a static seed)	Mine
2.2	#18	Render common student answers from deterministic templates instead of an AI call	Mine — depends on 2.1
2.3	#16	Flip the bilingual cache shortcut on for real users behind a 10% → 50% → 100% rollout flag	Mine — depends on 2.2
Exit criteria: PostHog shows cache_shortcut_hit events climbing; /api/health/cache shows fingerprint hit-ratio rising on definition / mcq / flashcard.

Wave 3 — SEO publishing pipeline (closes #11 family)
Bot management (#9) and the SEO routes (#11) are already live; this wave fills in the actual content + fixes plumbing.

Order	#	What	Owner
3.1	#20	Stop writing duplicate sitemap rows for SEO chapter pages	Mine — pure bug fix
3.2	#21	SQS consumer + IAM for seo-google-indexing queue so submissions actually fire in prod	Mine — Terraform + Lambda
3.3	#22	Push AEO answer cards to the edge cache	Mine
3.4	policy decision	How chapter notes/MCQs/flashcards get authored: human-written, fully AI, or hybrid + review queue	Yours — blocks 3.5
3.5	#19	Write the textual content for every chapter SEO page (the heaviest single ticket — could be weeks of generation + QA depending on 3.4)	Mine, gated by your 3.4
3.6	#11	Close parent once 3.1–3.5 merged	Mine
Exit criteria: every published chapter page has notes + MCQs + flashcards + summary + revision pack; Bing/Google Indexing API receives URLs daily; sitemap has no duplicates.

Wave 4 — Bedrock Indic embed completion (closes #27 follow-ups)
Order	#	What	Owner
4.1	#32	Backfill Assamese vectors onto the Bedrock route (one-shot Lambda)	Mine
4.2	#33	Side-by-side Assamese retrieval recall before/after Bedrock (admin tile)	Mine — depends on 4.1
4.3	#34	Reconcile estimated Bedrock spend vs the real AWS bill	Mine + you confirm AWS Cost Explorer access
Exit criteria: admin tile shows recall lift; INDIC_EMBED_MONTHLY_USD_SUBCAP actuals within 10% of estimate.

Wave 5 — Observability & ranking baseline (closes #15)
Has to come after Waves 1–4 so there's actually something to measure.

Order	#	What	Owner
5.1	#3	Voice-paywall + background-job events into PostHog	Mine
5.2	#28	Schedule the weekly SEO baseline + post to admin dashboard	Mine
5.3	#30	Export weekly SEO baseline history into the repo for offline diffing	Mine — depends on 5.2
5.4	#31	Show the weekly SEO baseline trend on the admin dashboard	Mine — depends on 5.2
5.5	#15	Close parent once 5.1–5.4 merged + 14 d of prod traffic captured	Mine
Exit criteria: admin dashboard has a single page that shows: cache hit-ratio per content_type, SEO baseline trend, voice-paywall conversion, prewarm success-rate.

Wave 6 — Architecture lock PARTIAL items (the hard ones)
These were left for last because each carries security/operational risk.

Order	Lock §	What	Owner	Risk
6.1	§6	Assamese corpus growth — keep as_translation_backfill.py running, raise coverage target to 85%	Mine	Low — already running
6.2	§4.2	Move OCR scratch from R2 → Azure Blob (per the lock) OR formally update the lock to keep R2	Mine + you decide which way	Medium — touches OCR pipeline
6.3	§13	Supabase-only auth cutover: delete legacy email/password endpoints in routes/auth.py and the JWT_SECRET user-session path in auth_deps.py	Mine, gated by your sign-off on user-impact window	High — biggest single open security item. Needs a forced-logout window for any existing JWT_SECRET sessions.
Exit criteria: all three matrix rows flip from PARTIAL → IMPLEMENTED.

Wave 7 — User-side housekeeping (only you can do these)
Can run in parallel with any wave above.

#	What	When
#4	Claim AWS Explore credits + clean throwaway resources	Whenever — needs your AWS console
Drop GOOGLE_OAUTH_CLIENT_SECRET from missing-secrets list	One-line confirmation it's retired (replit.md already says so)	Now
Decide on optional secrets: AZURE_SPEECH_*, AZURE_TRANSLATOR_*, ELEVENLABS_VOICE_ID, GCP_PROJECT_ID	Add them only if you want the corresponding fallback feature	Anytime
Wave 8 — Calendar-gated verification (cannot be accelerated)
#	What	Date
#29	Confirm cache hit-ratio target after 14 d of post-merge prod traffic	14 d after Waves 1–3 land
#35 / #36	Fill in the cache hit-ratio numbers (target ≥0.70 per content_type)	2026-05-23 (paired entries in architecture-matrix.json)
Suggested cadence
This week: Wave 0 (half a day) → Wave 1 (1–2 days). You unblock 3.4 (SEO content authoring policy) and 6.2 (R2 vs Azure Blob for OCR) in parallel so they don't gate later waves.
Next week: Wave 2 (cache shortcut rollout) → Wave 3 plumbing (3.1–3.3, 3.5 starts after your 3.4 decision).
Week 3: Wave 4 (Bedrock follow-ups), Wave 5 (observability) running in parallel.
Week 4: Wave 6 — start with 6.1, then 6.2, finish on 6.3 (Supabase cutover) once you've picked the maintenance window.
2026-05-23: Wave 8 fills happen automatically once the metric is read off CloudWatch.
Two decisions I need from you to avoid blocking later waves
3.4 SEO content authoring policy — human / AI / hybrid with review queue?
6.3 Supabase cutover window — pick a low-traffic hour for forcing all JWT_SECRET sessions to re-login.