work as a dev use sha key , github token , cloudflare api token and fix live deployment functioality audit Cloudflare Layer — Fully Connected to Cloud Run
Request path through Cloudflare: CHECK SENTRY LOGS , CLOUDUILD AND CLOUD RUN LOGS , CLOUDFLARE LOGS                                                        Root Causes Found & Fixed
Bug 1 — Gemini API credits depleted (429 RESOURCE_EXHAUSTED)
Every English chat request hit Gemini → "Your prepayment credits are depleted" → backend returned 503. This was ALL of the chat_upstream_http_error, stream failures, and circuit breaker events in Sentry.

Bug 2 — Wrong Sarvam auth header
sarvam_client.py used Authorization: Bearer sk_... — Sarvam's API requires api-subscription-key. Every Sarvam call was silently failing.

Bug 3 — No English fallback when Gemini fails
chat_service.call_llm and stream_llm both had fallback logic only for Assamese — English just re-raised the error. With Gemini down, English users got 503 with no recovery path.

All three fixed in commit e07b3165, deployed as revision syrabit-backend-00530-kjv.

✅ Live Verification (post-deploy)
POST /api/v1/chat/         → HTTP 200 | model=sarvam-30b | 13s
POST /api/v1/chat/stream   → SSE fallback=true/provider=sarvam | real chunks streaming
GET  /api/v1/health        → 200 healthy
GET  /api/v1/health/deep   → MongoDB healthy | Sarvam healthy | Vector search 197 topics
GET  /api/v1/content/search?q=thermodynamics → 3 results, score=0.86
GET  /api/v1/subscription/plans → 200

Sentry: 0 new errors in last 1 hour ✅

                                                    ⚠️ Remaining Items (need your action — not code issues)
Issue	Fix Required
Gemini 429 (core problem)	Add prepayment credits at ai.studio/projects or switch to Vertex AI billing
TTS 403	Grant roles/cloudtexttospeech.user to syrabit-backend-sa@... in GCP IAM
PostHog 401	Replace personal API key with project API key in Cloud Run env
Resend 401 (email)	Update RESEND_API_KEY in Cloud Run with a valid key
Chat latency 13s	Temporary (Sarvam-30b is a thinking model); resolves when Gemini credits are topped up