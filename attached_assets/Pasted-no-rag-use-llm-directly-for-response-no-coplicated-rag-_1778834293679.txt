no rag use llm directly for response , no coplicated rag pipeline , direct llm calls with the education specific syste prompt

39 minutes ago

Show less

Planning LLM integration simplification (22 seconds)
Let me quickly check what the frontend chat page calls and what the existing system prompts look like.


14 actions
Let me read the frontend chat page and existing prompt builder in parallel — then I'll write the simple direct route and wire it up.


4 actions
Let me find the actual send function in ChatPage.jsx and the direct Workers AI call path in parallel.


4 actions
Now I have the full picture. The frontend calls /api/ai/chat/stream as SSE. The cleanest move: create routes/ai_chat_direct.py with simple /ai/chat and /ai/chat/stream routes that go straight to Workers AI, then register it before the old complex router so it wins — zero frontend changes needed.

Let me read the SSE event format the frontend expects and the Workers AI call pattern in parallel.


4 actions
I have everything I need. Now I'll write the simple direct route and wire it up — no frontend changes required since I'll register it before the old router so it shadows /ai/chat and /ai/chat/stream.


2 actions
Now wire it into server.py before the old router, then test.


7 actions
Let me run the import smoke test and restart in parallel.


7 actions
Both endpoints are live and working. Let me take a screenshot to confirm the UI is rendering the streaming chat correctly.


2 actions
The chat page is rendering correctly and the backend is serving it cleanly. Here's what changed:

What was replaced: The 4,400-line RAG pipeline (Pinecone vector search → BM25 → web search fallback → multi-stage routing → provider chain fallback) is now bypassed entirely for both chat endpoints.

What runs instead (routes/ai_chat_direct.py, ~160 lines):

Accept the same request body the frontend already sends — no frontend changes
Build an AHSEC/SEBA/Degree-specific system prompt, injecting any board/class/subject/chapter context the frontend includes
Call Workers AI llama-3.3-70b-instruct-fp8-fast directly via httpx
Stream tokens back in the exact SSE format the frontend expects
Verified working:

POST /api/ai/chat → HTTP 200, real answer in under 3s
POST /api/ai/chat/stream → SSE streaming, tokens arrive as {"content": "chunk"} events, frontend renders them as they arrive
The new router is registered before the old complex one, so FastAPI routes the traffic to the simple path. The old complex code is still there and untouched — all its other endpoints (/ai/ocr-image, /ai/chat/typing, /v1/search, etc.) continue to work.