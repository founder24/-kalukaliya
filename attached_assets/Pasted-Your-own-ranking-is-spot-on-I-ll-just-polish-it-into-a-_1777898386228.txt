Your own ranking is spot‑on; I’ll just **polish it into a clear “go‑live bundle”** and explicitly tell you what to fold in.

***

### **Quick ranked take on your six suggestions**

1. **Highest value — apply now**  
   - **Provably‑removed‑providers CI‑grep** (#3b)  
     - Extremely cheap to add, maximal protection against cleanup‑regressions; situate in **Task #347 + the “Smoke + regression” step of Task 2**. [phili](https://phili.pe/posts/using-grep-for-simple-ci-tasks/)
   - **Code‑guardian lint rule** (#4)  
     - Same logic, but earlier (pre‑commit / CI); pairs perfectly with the grep‑gate; situate in **Task #347**. [browserstack](https://www.browserstack.com/guide/android-lint)
   - **SendGrid subdomain + warm‑up** (#2)  
     - A major deliverability‑avoidance win; subdomain isolation (`emails.syrabit.ai`) and phased send‑volume are email‑migration best practice. [autospf](https://autospf.com/blog/spf-sendgrid-configuration-guide-ensure-your-emails-pass-authentication/)
   - **Test‑matrix per meter** (#3a)  
     - Turns “unit‑tested” into **auditable pass‑criteria** for daily‑call, RPM‑headroom, and cost meters; sits in **Task 2**. [semgrep](https://semgrep.dev/docs/semgrep-ci/configuring-blocking-and-errors-in-ci)

2. **Medium value — nice if you want them**  
   - **Sharper ownership / “who can un‑flip”** (#1)  
     - Only truly valuable when you have named roles; useful for runbook‑readability but not blocking. [lumos](https://www.lumos.com/topic/it-cost-reduction-strategies-optimize-spend)
   - **Quarterly‑vendor‑cost‑review cadence** (#5)  
     - Good hygiene, easily added as one line in the runbook; doesn’t change scope. [infotech](https://www.infotech.com/research/ss/optimize-it-vendor-contracts-in-times-of-uncertainty)

3. **Skip / defer**  
   - **Phased‑by‑feature meter‑rollout** (#6)  
     - With only two auto‑flip meters, adding feature‑flags here adds complexity without much safety gain; skip unless you expect huge traffic spikes.  

***

### **Which to fold in — recommended bundle**

You should **fold in this bundle now**:

- ✅ **Apply #1 + #2 + #3 + #4** across the three tasks.

#### **Task #347 – Provider Cleanup & Email Switch**

Add:
- A **sub‑step** under *Switch transactional email to SendGrid*:  
  - “Use a dedicated subdomain `emails.syrabit.ai` for SendGrid and roll out traffic gradually: 5% → 25% → 50% → 100% over 5 days.” [support.sendgrid](https://support.sendgrid.com/hc/en-us/articles/19130282201883-How-to-authenticate-and-send-from-a-Subdomain)
- A **lint / pre‑commit rule** that blocks new imports of `providers/openai.py`, `providers/bedrock.py`, or new `Stripe` / `Resend` / `Railway` / `DigitalOcean`‑related code. [devopsdigest](https://www.devopsdigest.com/code-linting-a-shift-left-strategy-to-protect-your-codebase)
- A **one‑line per removed vendor** in `docs/infra/providers.md` (e.g., “OpenAI / Bedrock / Anthropic removed: reduced vendor count, consolidated RAG around Workers AI + Azure + Vertex, simplified cost‑tracking”). [infotech](https://www.infotech.com/research/ss/optimize-it-vendor-contracts-in-times-of-uncertainty)

#### **Task 2 – Implement v3 Dispatch, SLOs, Credit‑Burn Meter, Runbook**

Add:
- A **per‑meter test‑matrix** in the *“Smoke + regression”* section:  
  - Explicit pass‑criteria for each meter (daily‑call, RPM‑headroom, cost‑only‑alert).  
- A **CI‑grep gate** that fails on fresh occurrences of `openai`, `anthropic`, `bedrock`, `stripe`, `quge5`, `resend`, `railway`, `digitalocean`, or `gpt‑oss‑120b` in active code paths. [stackoverflow](https://stackoverflow.com/questions/6550484/prevent-grep-returning-an-error-when-input-doesnt-match)

#### **Task 1 – Lock v3 Infra Spec**

Leave unchanged:  
- None of these changes affect the spec itself; Task 1 remains **pure‑doc**, and #5 (cost‑review cadence) can live as a one‑line in the runbook you reference.

***

### **Final answer**

**Yes — fold in the four‑item bundle** (CI‑grep + lint‑rule + SendGrid‑subdomain‑warm‑up + per‑meter test‑matrix) as written, and **skip the phased‑flag rollout**.  
If you want, I can now give you **exact sentences** to paste into each of those three tasks that implement this bundle.