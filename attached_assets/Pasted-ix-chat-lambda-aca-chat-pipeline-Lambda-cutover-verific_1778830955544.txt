ix(chat+lambda+aca): chat pipeline; Lambda cutover verification; ACA R2/Razorpay secrets

19df70d

Restore All

=== Chat pipeline ===

contextvars fix: mark_mongo_read() in parent context before asyncio gather/create_task
Exa API fix (llm.py): search(..., text=True) → search(..., contents=ContentsOptions(text=True))
requirements.txt: duckduckgo_search==8.1.1 → ddgs>=9.14.3
=== ACA deploy secret regression fix (azure-container-apps-deploy.yml) ===

R2 and RAZORPAY_WEBHOOK_SECRET missing from ARM PATCH secrets/env arrays. Added:
r2-access-key-id, r2-secret-access-key, r2-bucket-name, r2-endpoint-url,
razorpay-webhook-secret to both the secrets array and the container env array.
=== Lambda cutover verification hardening ===
manifest.json:

Removed premature cutover_verified_at stamps (must be written by verify workflow post-run)
Added smoke_verified_at/smoke_verified_by/smoke_verification_run fields for run #25898826581
cutover_verified_at reserved for post-2026-05-16 scheduled-run evidence
verify-lambda-cutover.yml:

Schedule: 2026-05-16 02:30 → 2026-05-18 02:30 UTC (after first seo_baseline
post-cutover run; seo_baseline is weekly Monday, 2026-05-16 is Saturday)
Metric window bug fixed: both PrewarmSuccessRate and SEO baseline steps used
SINCE_ISO+6h as END — seo_baseline metrics (published 2026-05-18) were outside
that window. Now uses datetime.utcnow() as END with period=691200s (8 days).
Dead-code removed: SEO_BASELINE_WINDOW_EPOCH computed and passed to check_function
but the CW query still used outer SINCE_EPOCH. Removed the parameter entirely;
describe-log-streams already correctly filters lastEventTimestamp >= SINCE_EPOCH.
check_function hardened: hard-fail on Runtime.ExitError, Task failed:, errorType.*Error,
Unhandled exception, errorMessage, "status":"error", [ERROR] app-level markers before
REPORT/success check. Was: REPORT-only with errors as warning.
INSUFFICIENT_DATA alarm state → hard fail (was warning)
Mongo chapter_faqs freshness + Redis aic:fp:* TTL freshness (stale → hard fail)
invoke-lambda-smoke.yml:

Removed secret-leaking echo → metadata-only (length/type/is_uri)
Per-function scheduled evidence, EventBridge ENABLED check
Mongo/Redis freshness hard fails; metpass/MET == 'true' (not != 'false')
seed-mongo-url-aws-sm.yml:

Removed "Current value (first 60 chars): ${CURRENT:0:60}" secret-leaking echo
→ replaced with safe metadata-only logging (length, is_uri, is_placeholder, is_not_found)