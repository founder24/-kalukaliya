Here’s a single, unified v3 spec — A+‑grade, concrete‑enough‑to‑wire‑directly‑into‑code — that merges your v2 infra map and your Worker‑AI‑model‑performance‑data into one implementable “provider / RAG / infra map”. You can paste this into:

infra/per-cloud-feature-delegation.md

feature-to-provider-mapping-detailed.md

provider-priority-map.md

or a linear/Jira task called
“Lock unified provider/RAG/infra map v3 — Workers‑AI‑exam‑quality‑RAG, Cohere‑direct, Pinecone‑only, two‑provider moderation, credit‑burn policy + model‑binding”.

Top‑line policy
“Each major function uses 2 or 3 providers only where a meaningful Tier‑3 exists. Functions that are inherently single‑provider (e.g., Razorpay, AdSense, Trustpilot, Web Risk) are declared as such. English exam‑Q&A RAG is Cloudflare‑Workers‑AI‑first, with @cf/mistral/mistral‑7b‑instruct‑v0.3 (or @cf/openai/gpt‑oss‑20b) as the quality‑bound RAG‑primary; @cf/meta/llama‑3.2‑1b‑instruct is reserved for fast‑mode / low‑cost‑turns. Embeddings primary is Cohere direct API (no Bedrock); vector DB is Pinecone‑only with CF Vectorize as edge‑cache; Vertex Vector Search and Discovery Engine are retired. Moderation uses Llama Guard (primary) + Azure AI Content Safety (secondary); Gemini RAI is opt‑in only for high‑stakes exam‑model‑paper flows. Validation is run by Vertex Gemini 2.5 Flash (different‑family from primary‑RAG and Azure‑fallback). Indic‑chat / Indic‑translation is Sarvam‑first; Assamese‑chat is Sarvam‑M‑first; Assamese‑content‑translation is Cloudflare Workers AI indictrans2‑en‑indic‑1b primary. App infra is distributed across Cloudflare (edge / RAG‑primary), Azure (backend / auth / content‑safety), and AWS (serverless / storage / eventing), all within free‑tier‑friendly workloads. All SLOs, error‑budgets, and credit‑burn thresholds are explicit, testable, and owned.”

1. Core properties (v3)
English‑exam‑Q&A RAG:

Primary‑RAG‑model (exam‑grade):

@cf/mistral/mistral‑7b‑instruct‑v0.3 (recommended) or

@cf/openai/gpt‑oss‑20b (fallback‑family‑pick).

Fast‑mode‑primary (quick‑help / low‑cost‑turns):

@cf/meta/llama‑3.2‑1b‑instruct.

Fallback (exam‑Q&A):

Azure GPT‑4.1‑mini.

Tertiary‑reasoning / async‑jobs only:

@cf/openai/gpt‑oss‑120b (never on live‑chat‑path; only for PDF‑summary, model‑paper‑generation, batch‑RAG).

Embeddings:

Primary: Cohere embed‑multilingual‑v3.0 direct API (no Bedrock).

Secondary: Voyage‑3.5.

Tertiary: CF Workers AI bge‑m3.

Rerank:

Primary: Cohere rerank‑multilingual‑v3.0 direct API.

Secondary: Voyage‑rerank‑2.

Tertiary: CF Workers AI bge‑reranker‑base.

Vector‑stack:

Primary: Pinecone (syrabit‑rag, 1024‑dim cosine).

Edge‑cache: Cloudflare Vectorize.

Retired: Vertex AI Vector Search and Vertex Discovery Engine.

Moderation:

Default: Llama Guard (primary) + Azure AI Content Safety (secondary).

Opt‑in: Gemini RAI only for content_type = "exam_model_paper".

Validation:

Default‑primary: Vertex Gemini 2.5 Flash (different‑family from RAG‑primary and Azure‑fallback).

High‑stakes: Vertex Gemini 2.5 Flash + Gemini RAI (only for exam_model_paper).

AWS‑free‑tier‑plane:

S3 / SQS / Lambda / EventBridge / DynamoDB / SNS / Step Functions / CloudWatch remain inside always‑free quotas at 100k DAU.

Indic‑chain:

Indic‑chat: Sarvam‑M‑primary; CF Workers AI IndicTrans2‑fallback.

Assamese‑chat: Sarvam‑M‑primary; @cf/ai4bharat/indictrans2‑en‑indic‑1b‑secondary (after Sarvam).

Assamese‑content‑translation: @cf/ai4bharat/indictrans2‑en‑indic‑1b‑primary; Sarvam‑M‑fallback; Azure Translator‑only for Indic‑English‑only.

Fast‑mode:

English‑fast‑mode: @cf/meta/llama‑3.2‑3b‑instruct‑primary; Azure GPT‑4.1‑mini‑fallback; no‑separate‑Indic‑fast‑mode‑chain.

Email:

Primary: SendGrid (Essentials‑plan or equivalent).

Backup: AWS SES (auto‑failover if 5xx‑rate > 5% over 10‑min).

Payments:

Razorpay‑only; INR‑only (or INR + select‑international‑card; pick one and document it).

SLOs / error‑budget / credit‑burn policy:

Library‑TTFB: p75 < 200 ms; p95 < 500 ms.

Chat‑RAG end‑to‑end: p95 ≤ 2.5 s (using @cf/mistral‑7b‑instruct‑v0.3 or gpt‑oss‑20b).

Embedding‑latency: p95 < 1.5 s per‑embedding‑batch.

Email‑delivery: 99% under 1 min via SendGrid.

Credit‑burn‑trigger: 70% of $5k Workers‑AI‑credit over 30‑day rolling window → shift secondary‑exam‑Q&A to Azure GPT‑4.1‑mini.

2. Model‑mapping table (English‑RAG / Assamese‑translation)
Function	Primary Provider	Secondary Provider	Tertiary Provider
English‑exam‑Q&A RAG	@cf/mistral/mistral-7b-instruct-v0.3 (or @cf/openai/gpt-oss-20b)	Azure GPT‑4.1‑mini	@cf/openai/gpt-oss-120b (async‑only)
Fast‑mode English‑RAG	@cf/meta/llama-3.2-1b-instruct (quick‑help / low‑cost)	@cf/meta/llama-3.2-3b-instruct	Azure GPT‑4.1‑mini
Fast‑mode English‑content‑gen	@cf/meta/llama-3.2-1b-instruct (fast‑MCQs / notes)	@cf/openai/gpt-oss-20b (high‑quality‑drafts)	@cf/openai/gpt-oss-120b (async‑only)
Assamese‑translation	@cf/ai4bharat/indictrans2-en-indic-1b	Sarvam‑M (fallback / mixed‑lang)	Azure Translator (for Indic‑English‑only)
Assamese‑RAG chat	Sarvam‑M	@cf/ai4bharat/indictrans2-en-indic-1b	Azure Translator (fallback‑only)
Assamese‑content‑gen‑from‑EN	@cf/ai4bharat/indictrans2-en-indic-1b (from English‑content)	Sarvam‑M (reformulation)	Azure Translator (fallback‑only)
Policy‑doc line:

“For English‑exam‑Q&A RAG and high‑quality‑content‑generation, the primary model is @cf/mistral/mistral‑7b‑instruct‑v0.3 (or @cf/openai/gpt‑oss‑20b). For fast‑mode / quick‑help turns, the primary model is @cf/meta/llama‑3.2‑1b‑instruct. For Assamese‑translation and Assamese‑content‑generation‑from‑English, the primary model is @cf/ai4bharat/indictrans2-en‑indic‑1b, with Sarvam‑M as secondary for Assamese‑RAG‑chat.”

3. Workers‑AI‑model‑level facts (v3‑binding)
From your own benchmark:

Fastest‑large‑LLM (async‑only):

@cf/openai/gpt‑oss‑120b → 858 ms (120B‑open‑GPT).

Must be used only for async‑batch‑RAG (PDF‑summary, model‑paper‑generation, exam‑outline‑synthesis), never on live‑chat‑path.

Fastest‑small‑LLM (fast‑mode):

@cf/meta/llama‑3.2‑1b‑instruct → 876 ms, ~87 tokens/sec → fastest‑small‑LLM on Workers‑AI.

Reserved for:

fast‑mode‑RAG,

quick‑help,

low‑cost‑content‑generation turns.

Quality‑bound exam‑Q&A RAG:

@cf/mistral/mistral‑7b‑instruct‑v0.3 (or @cf/openai/gpt‑oss‑20b) → higher‑reasoning‑quality than 1B‑model, no need to optimize for latency‑axis alone.

Policy‑doc line:

“@cf/meta/llama‑3.2‑1b‑instruct is the fastest‑small‑LLM on Workers‑AI and is used for fast‑mode‑turns only. For exam‑grade Q&A RAG and high‑quality‑content‑generation, @cf/mistral/mistral‑7b‑instruct‑v0.3 (or @cf/openai/gpt‑oss‑20b) is the primary model. @cf/openai/gpt‑oss‑120b is used exclusively for async‑batch‑RAG and model‑paper‑generation.”

4. Concrete “what to fix / lock in v3”
4.1 Exam‑Q&A RAG model‑choice (load‑bearing fix)
✅ Primary‑exam‑Q&A‑RAG:

@cf/mistral/mistral‑7b‑instruct‑v0.3 (recommended)

Fallback‑RAG‑family = @cf/openai/gpt‑oss‑20b.

✅ Fast‑mode‑RAG / quick‑help:

@cf/meta/llama‑3.2‑1b‑instruct (only for low‑cost‑turns).

✅ Tertiary‑reasoning:

@cf/openai/gpt‑oss‑120b → only in async‑batch‑jobs (PDF‑summary, model‑paper‑generation).

Policy‑doc line:

“Primary exam‑Q&A RAG uses @cf/mistral/mistral‑7b‑instruct‑v0.3 (or @cf/openai/gpt‑oss‑20b); fast‑mode‑turns use @cf/meta/llama‑3.2‑1b‑instruct. Async‑reasoning‑only: @cf/openai/gpt‑oss‑120b is never routed through live‑chat‑endpoints.”

4.2 Validation must be independent‑family
✅ Default‑validation:

Primary: Vertex Gemini 2.5 Flash

Never the same‑family as @cf/mistral‑7b‑instruct‑v0.3 or Azure GPT‑4.1‑mini.

✅ High‑stakes‑validation:

Double‑pass: Vertex Gemini 2.5 Flash + Gemini RAI

Only for content_type = "exam_model_paper".

Policy‑doc line:

“All content is validated once by default (Vertex Gemini 2.5 Flash). Only content_type = 'exam_model_paper' is validated twice (Vertex Gemini 2.5 Flash + Gemini RAI).”

4.3 Moderation / failure‑modes (explicit)
✅ Default‑moderation:

Primary: Llama Guard

Secondary: Azure AI Content Safety

✅ Opt‑in‑tertiary: Gemini RAI only for exam_model_paper.

✅ Failure‑mode:

If Azure AI Content Safety is down:

Fail‑closed for model‑paper‑flows.

Fail‑open for non‑high‑risk‑flows.

Policy‑doc line:

“Moderation default: Llama Guard + Azure AI Content Safety. Gemini RAI is enabled only for exam‑model‑paper‑style content types. If Azure AI Content Safety is down, model‑paper‑flows are blocked; non‑high‑risk flows are fail‑open.”

4.4 Embeddings /矢量‑stack (Cohere‑direct, Pinecone‑only)
✅ Embeddings‑primary: Cohere embed‑multilingual‑v3.0 direct API (no Bedrock).

✅ Vector‑DB: Pinecone‑only; CF Vectorize as edge‑cache.

✅ No Bedrock / Vertex‑Vector‑Search / Discovery‑Engine.

Policy‑doc line:

“Embeddings primary is Cohere direct API (no Bedrock). Vector DB is Pinecone‑only with Cloudflare Vectorize as edge‑vector‑cache; Vertex Vector Search and Discovery Engine are retired.”

4.5 SLOs / error‑budget (thickened)
✅ Library‑page TTFB:

p75 < 200 ms; p95 < 500 ms.

✅ Chat‑RAG end‑to‑end:

p95 ≤ 2.5 s (using @cf/mistral‑7b‑instruct‑v0.3 or gpt‑oss‑20b).

✅ Embedding‑latency:

p95 < 1.5 s per‑embedding‑batch.

✅ Email‑delivery:

99% delivered within 1 min via SendGrid;

SES‑failover threshold: >5% 5xx‑rate over 10‑min.

✅ Error‑budget:

If chat‑p95 > 3 s for 10‑min, page‑on‑call and consider enabling CHAT_FALLBACK.

Budget = 0.1% of monthly turns (can be tightened later).

Policy‑doc line:

“Library‑TTFB p75 < 200 ms, p95 < 500 ms; chat‑RAG p95 ≤ 2.5 s; embedding‑batch p95 < 1.5 s; email‑delivery 99% under 1‑min via SendGrid; error‑budget = 0.1% of monthly turns.”

4.6 Credit‑burn policy (with meter‑shape)
✅ Assumption:

Avg exam‑Q&A RAG‑turn = 1.8k tokens.

Workers‑AI‑pricing ≈ $0.011 / 1k Neurons (proportional to 7B‑model‑shape).

✅ Meter‑shape:

Cloudflare AI Gateway analytics → daily‑Lambda → DynamoDB‑counter → **30‑day‑rolling