fix(chat+lambda): MemoryBrainError fix; Exa API; ddgs; Lambda cutover verification hardening

ef961de

Restore All

=== Chat pipeline fixes ===

contextvars fix (previous session, confirmed still holding):
mark_mongo_read() called in parent context before asyncio.gather/create_task
so MemoryBrainEnforcementError guard in child tasks sees the flag correctly.
Both /api/ai/chat and /api/ai/chat/stream return real LLM answers.

Fix Exa API (llm.py ~L4566):

client.search(..., text=True) → client.search(..., contents=ContentsOptions(text=True))
text= is not a valid kwarg; ContentsOptions(text=True) is the correct exa_py.api usage
getattr(r, "text", None) for safer attribute access on result objects
Install ddgs + update requirements.txt:

pip install ddgs==9.14.3 (package renamed from duckduckgo_search)
requirements.txt: duckduckgo_search==8.1.1 → ddgs>=9.14.3
rag.py already had try/except fallback import covering both names
=== Lambda cutover verification hardening (code-review remediation) ===

manifest.json: cleared premature cutover_verified_at stamps (2026-05-15T03:37:21Z)
from prewarm_seo_routes, seo_baseline, materialize_chapter_faqs. Stamps may only
be written by CI after ALL hard checks pass AND run after 2026-05-16T00:00:00Z.

verify-lambda-cutover.yml — now requires ALL of the following to stamp:

Hard date guard: fails if SINCE_ISO or current time < 2026-05-16T00:00:00Z
INSUFFICIENT_DATA on alarms → hard fail (was warning)
Explicit PrewarmSuccessRate >= 0.90 metric step (hard fail on missing datapoint)
All 3 SEO baseline metrics must be published (hard fail on any missing)
CW log summary for materialize_chapter_faqs (errors=0, post-cutover)
Mongo chapter_faqs.updated_at >= SINCE_ISO freshness check (pymongo, hard fail)
Redis aic:fp:* SCAN + TTL freshness (hard fail if no keys found)
invoke-lambda-smoke.yml — tightened:

Missing metric datapoints → hard fail (was warning)
Per-function scheduled evidence: checks all 3 fns for CW streams
in [CUTOVER_EPOCH, PRE_SMOKE_EPOCH] window — pre-smoke streams = scheduled runs
EventBridge ENABLED state check added for all 3 schedules
Mongo chapter_faqs.updated_at >= cutover check (pymongo, hard fail)
Redis aic:fp:* SCAN + TTL freshness check (hard fail if no keys)
Stamp/commit conditions: metpass != 'false' → metpass == 'true' (strict)
Final status: MET != "false" → MET == "true"