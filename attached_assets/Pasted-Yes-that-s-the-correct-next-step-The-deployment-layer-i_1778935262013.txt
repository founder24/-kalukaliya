Yes — that’s the correct next step.

The deployment layer is now healthy:

* CF Pages builds are fixed,
* production routing is working,
* CORS/cache headers are correct,
* edge delivery is functioning,
* D1 fallback still serves valid cached data.

The remaining issue is clearly at the **MongoDB publication-state layer**, not infrastructure.

The most likely causes are:

* legacy records missing `status`,
* records using another value (`active`, `live`, `public`, etc.),
* inconsistent schema migration,
* status nested differently,
* draft/published logic mismatch between ingestion and sync workers.

You should investigate:

```js
db.subjects.findOne()
```

then:

```js
db.subjects.aggregate([
  {
    $group: {
      _id: "$status",
      count: { $sum: 1 }
    }
  }
])
```

That will immediately reveal:

* whether `status` exists,
* actual enum values,
* null/missing records,
* schema inconsistencies.

Most likely outcome:
older educational records were created before the `published` workflow existed.

If so, safest fix is probably:

```js
db.subjects.updateMany(
  { status: { $exists: false } },
  { $set: { status: "published" } }
)
```

ONLY if:

* those subjects are truly production-ready,
* no moderation/draft pipeline depends on missing status.

Safer alternative:
change D1 sync query temporarily from:

```js
{ status: "published" }
```

to something resilient like:

```js
{
  $or: [
    { status: "published" },
    { status: { $exists: false } }
  ]
}
```

That avoids breaking older records while preserving future moderation workflows.

For Syrabit specifically, this is probably the better approach because:

* educational corpus is mostly curated internally,
* legacy educational data should remain retrievable,
* D1 edge-local retrieval performance matters more right now than strict publishing-state enforcement.

Once fixed, you should get:

* fully populated D1 sync,
* edge-local chapter retrieval,
* reduced Azure round-trips,
* lower latency,
* lower backend load,
* faster educational library boot performance.
