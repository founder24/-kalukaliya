This audit‑find‑and‑fix is **exactly right**; you’re already operating at a **serious‑engineering‑level** on cross‑task consistency. Here’s a **tight, “apply‑as‑is‑with‑minor‑tweaks”** version of your **recommendation** you can drop into your internal RFC / ticket‑comments:

***

### ✅ Apply fixes 1–4 (surgical edits, no scope‑change)

1. **Fix 1: Correct `#337` wording (cannot be “cancelled”)**  
   - In **Task #347**:  
     - Where you say “Cancel Task #337 (Enable AWS‑native Bedrock/Polly/Transcribe/Textract/etc.)” → change to:  
       > “Reverse the AWS‑native Bedrock/Polly/Transcribe/Textract/Translate enablements from #337 (already covered in steps 2–3); leave #337 in **MERGED** state.”  
   - Keep **“Cancel Task #341”** as‑is (since #341 is still in PROPOSED state).  

2. **Fix 2: Workers‑AI Indic‑model mismatch (`#347` vs `#359`)**  
   - In **Task #347**, replace:  
     > `@cf/meta/llama-3.1-8b-instruct Indic‑tuned, or whichever Workers AI Indic model is currently live — pick at implementation time`  
     with two explicit slugs:  
     - For **Asamese‑translation**:  
       > `@cf/ai4bharat/indictrans2-en-indic-1b` (translation model, as in v3). [developers.cloudflare](https://developers.cloudflare.com/workers-ai/models/)
     - For **Indic‑chat‑fallback**:  
       > `@cf/meta/llama-3.1-8b-instruct` (multilingual‑chat‑model, Indic‑friendly). [developers.cloudflare](https://developers.cloudflare.com/workers-ai/models/llama-3.1-8b-instruct/)

3. **Fix 3: Add `Grok` / `xai` to `#347` “Done‑looks‑like” grep list**  
   - In **Task #347 “Done looks like”**, extend the grep‑list from:  
     > `OpenAI, Anthropic, Bedrock, Stripe, Quge5, Resend, Railway, DigitalOcean`  
     to:  
     > `OpenAI, Anthropic, Bedrock, Stripe, Quge5, Resend, Railway, DigitalOcean, Grok, xai`  
   - This now matches both the **Removals** section and the **CI‑grep‑gate** in `#360`.

4. **Fix 4: Lock the two remaining “pick one” ambiguities in `#359`**  
   - In **Task #359** “Done looks like” / acceptance‑criteria:
     - **Razorpay scope**:  
       - Where you say “Razorpay scope is locked (INR‑only or INR + selected international cards — pick one)”:  
         → lock it to **INR‑only**, since `#347` already says “Razorpay becomes the sole payment gateway (India‑only is acceptable for now)”. [zoho](https://www.zoho.com/checkout/faq/payment-gateways/razorpay-international-cards.html)
     - **Fast‑mode‑primary (1b vs 3b)**:  
       - Where you say “Fast‑mode‑primary is locked (1b vs 3b — pick one)”:  
         → lock it to **`@cf/meta-llama/Llama-3.2-3B-Instruct`**, since `#347` already commits to that as the **“Fast‑mode” model**.  
     - **Rust‑core “ambiguity” line**:  
       - Where you say “Rust core is named explicitly as a separate Azure Container App (not co‑deployed with Python)”:  
         → change from “decide” to **“confirm”**, since this is already specified in `#347` and your cloud‑map.

***

### ✅ Confirmation on “no‑action” items

- **`#335` is MERGED but a dependency of `#347`** → that’s fine; MERGED tasks are valid dependencies, and the “Coordinate with #335” step becomes a **no‑op confirmation**.  
- **`#347` “no‑contradicting‑docs” vs `#359` grep‑sweep** → mild redundancy but harmless; both ensure the same consistency bar.  
- **`SENDRGRID_API_KEY` to both CF‑Worker and Azure‑Container‑Apps** → correct, since `email-worker` is a CF‑Worker and you also need it for FastAPI‑triggered mail.

***

### Final recommendation wording you can paste

> “Apply fixes 1–4:  
> 1. Update `#347` to say that `#337` should be *reversed* (not *cancelled*),  
> 2. Make `@cf/ai4bharat/indictrans2-en-indic-1b` (translation) and `@cf/meta/llama-3.1-8b-instruct` (Indic‑chat‑fallback) explicit in `#347`,  
> 3. Add `Grok` / `xai` to the `grep`‑list in `#347`’s acceptance‑criteria, and  
> 4. In `#359`, lock Razorpay to **INR‑only**, fast‑mode to **3B**, and downgrade the Rust‑core line to **confirm** instead of **decide**.  
>   
> These are **surgical edits, no scope‑change**; once committed, `#347`, `#359`, and `#360` will be fully consistent and auditable.”

If you want, I can now give you **the exact diff‑style lines** for each of these four edits (ready‑to‑paste into your internal task‑descriptions).