You're absolutely right. The attached script was a generic Vite optimization checklist that ignored your production architecture and the precise, battle-tested work already shipped across Tasks #391, #395, #496, and #639. Applying it verbatim would actively regress your scores, conflict with your edge infrastructure, and waste build cycles on no-ops.

Your diagnosis of the real bottleneck is correct: **the homepage isn't prerendered like `/library`, so it ships the full SPA preload set on mobile.** That's the exact 26-point gap. No amount of image compression, CSS inlining, or vendor chunk tweaking will close it until the homepage's critical rendering path matches your proven library prerender strategy.

### ✅ Recommendation: Keep Task #453 exactly as scoped.
The right path forward is strictly architecture-aware:
1. **Homepage prerender parity** – Apply the `prerender-library.mjs` pattern to `/` to strip non-critical preloads on mobile initial render.
2. **Measurement-driven validation** – Use your `PAGESPEED_LIBRARY_*.md` template for before/after PSI runs on both form factors, with explicit `/library` regression checks.
3. **Preserve existing optimizations** – Keep the granular chunk splits, custom `sw.js` (admin/crawler bypass), CF Image Resizing, esbuild minification, and `font-display: swap`. These are already dialed in and production-proven.
4. **Third-party deferral extension** – Apply the Task #391 pattern to any remaining CF Web Analytics injectors or non-critical scripts.

### 📝 Documentation Cleanup Task
If you'd like, I'll draft a ready-to-commit follow-up task that:
- Removes generic "Vite perf checklist" assumptions from onboarding/contributor docs
- Explicitly documents your architecture constraints (CF edge, custom SW, prerender parity requirement, chunk split strategy)
- Adds a pre-commit/pre-PR checklist that blocks generic script pastes and mandates PSI measurement reports

Just say the word and I'll output it in your exact task format. This keeps the workflow focused on what actually moves the needle: prerender parity, precise measurement, and zero regression on existing wins.